import { ArchivedRecords } from "@/features/archived/components/archived-records";

export const metadata = {
  title: "Arquivados",
};

export default function ArchivedPage() {
  return <ArchivedRecords />;
}
